import React,{useCallback,useEffect,useState}from'react';
import{BrowserRouter,Routes,Route,Navigate}from'react-router-dom';
import{supabase}from'./lib/supabaseClient';
import Login from'./pages/Login';import Dashboard from'./pages/Dashboard';import Users from'./pages/Users';import Reports from'./pages/Reports';import Content from'./pages/Content';import Sidebar from'./components/Sidebar';
interface AdminSession{userId:string;isAdmin:boolean}const ADMIN_EMAILS=new Set(['sheenomatp@gmail.com']);
export default function App(){const[session,setSession]=useState<AdminSession|null|undefined>(undefined);const applyAuthSession=useCallback(async(authSession:any)=>{const user=authSession?.user;if(!user){setSession(null);return}const email=(user.email??'').toLowerCase();if(ADMIN_EMAILS.has(email)){setSession({userId:user.id,isAdmin:true});return}const{data:profile}=await supabase.from('profiles').select('role').eq('id',user.id).maybeSingle();setSession({userId:user.id,isAdmin:profile?.role==='admin'})},[]);useEffect(()=>{let alive=true;const boot=async()=>{const{data}=await supabase.auth.getSession();if(alive)await applyAuthSession(data.session)};void boot();const{data:listener}=supabase.auth.onAuthStateChange((_e,s)=>{if(alive)void applyAuthSession(s)});return()=>{alive=false;listener.subscription.unsubscribe()}},[applyAuthSession]);const handleLoggedIn=useCallback(async()=>{const{data}=await supabase.auth.getSession();await applyAuthSession(data.session)},[applyAuthSession]);if(session===undefined)return <div className="admin-loading">Loading…</div>;if(!session||!session.isAdmin)return <BrowserRouter><Login onLoggedIn={handleLoggedIn} deniedNonAdmin={!!session&&!session.isAdmin}/></BrowserRouter>;return <BrowserRouter><div className="admin-shell"><Sidebar onLogout={()=>supabase.auth.signOut()}/><main className="admin-shell__content"><Routes><Route path="/" element={<Dashboard/>}/><Route path="/users" element={<Users adminUserId={session.userId}/>}/><Route path="/reports" element={<Reports/>}/><Route path="/content" element={<Content/>}/><Route path="*" element={<Navigate to="/" replace/>}/></Routes></main></div></BrowserRouter>}
